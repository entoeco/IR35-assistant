# Phase 1 dependencies. Pinned exactly — an unpinned dependency makes the
# "reproducible from config" claim false the moment a transitive release
# changes behaviour.
PyYAML==6.0.3
pytest==9.1.1

# Read only. Used once, in Phase 0, to extract the schema from the ESQ
# template. Not needed to regenerate the corpus.
openpyxl==3.1.5
